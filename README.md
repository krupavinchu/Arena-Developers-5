# Advanced Real-time Analytics Dashboard

This project is organized from the supplied project specification.

## Modules
- analytics-backend — Spring WebFlux reactive backend
- analytics-frontend — React dashboard placeholder
- analytics-mobile — React Native app containing the supplied mobile sample
- data-pipeline — data ingestion/processing placeholder
- ml-models — ML integration placeholder
- search-service — Elasticsearch integration placeholder
- alerting-service — notification placeholder
- serverless-functions — serverless prediction placeholder
- infrastructure — deployment/infrastructure placeholder
- monitoring — monitoring configuration placeholder
- docs — project documentation

## Important
The uploaded specification provides sample backend/mobile code and a target architecture. Some services are described but not fully implemented in the source material, so this ZIP preserves the supplied code and creates clear module boundaries rather than inventing missing production implementations.

See `docs/SOURCE_SPECIFICATION.md` for the supplied requirements.
